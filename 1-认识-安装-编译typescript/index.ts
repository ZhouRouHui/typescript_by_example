

/**
 * ts 的介绍
 * 
 * 1. TypeScript 是微软开发的开源的编程语言，它是 javascript 的超集（兼容 es6，es5 的语法且自己还有扩展的语法特性）。
 * 2. Typescript 的文件扩展名为 .ts，浏览器无法识别和运行 ts 文件，所以需要将 ts 文件编译转换成 js 文件。
 * 3. Ts 文件编译成 js 文件需要 typescript 环境的支持，需要在本地安装 typescript 的环境，
 *      安装命令：`yarn [global] add typescript` 或 `npm install -g typescript`，
 *      然后运行 `tsc filename.ts`，就会将 ts 文件编译成 js 文件，编译成的 js 文件在当前目录。
 */


let str: string = 'hello  world';
